{
  "name": "blueprint-diagrams",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "static": "turbo run --filter=static",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.4",
    "typescript": "5.9.2"
  },
  "license": "AGPL-3.0",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=18"
  }
}
